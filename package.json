{
  "name": "semantic-autocomplete-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Fastify + WebSocket server for semantic autocomplete with Redis caching and embedding",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prepare:embeddings": "node prepare_embedding.js"
  },
  "dependencies": {
    "fastify": "^4.22.0",
    "ws": "^8.13.0",
    "ioredis": "^5.3.2",
    "openai": "^4.8.0",
    "dotenv": "^16.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}
